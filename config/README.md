# 配置目录

这里保存可以进入源码的非秘密配置：

- `datasets/`：公开数据集清单、来源和校验值。
- `ontology/`：农业本体和图片实体别名。
- `prompts/`：可审查的系统提示词模板。

API Key、模型凭据、管理员密码和私有地址必须写入根目录 `.env.ai`，不要在本目录新增秘密配置。本地验收依赖的随机凭据由启动脚本生成到被忽略的 `var/acceptance/infra.env`，无需手工编辑，也不得用于生产环境。
