# 前端静态文件发布后可参考这份 Nginx 配置。聊天流使用普通 HTTP SSE。

server {
  listen 8080;

  location / {
    # root D:/AgriGraph/frontend/dist;
    root html;
    index index.html index.htm;
  }

  location /api/ {
    proxy_pass http://127.0.0.1:8088;
  }
}
