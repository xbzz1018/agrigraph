# AgriGraph 智农图谱服务平台：项目说明与源码阐述

## 源码快速导航

| 模块 | 源码入口 | 重点阅读 |
| --- | --- | --- |
| 应用启动 | [app/main.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/main.py) | `create_app`、生命周期、Router 注册、统一异常 |
| 依赖装配 | [core/container.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/core/container.py) | `AppContainer.build`、外部客户端创建与关闭 |
| 配置管理 | [core/settings.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/core/settings.py)、[.env.ai.example](F:/code/homework/project/agrigraph-evidence-qa/.env.ai.example) | `Settings.from_env`、模型/存储/超时/TTL 配置 |
| LangGraph 图 | [agents/chat_graph.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/chat_graph.py) | `MainAgentGraph._build`、固定节点顺序、SQLite Checkpoint |
| 工作流状态 | [agents/schemas.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/schemas.py) | `AgentState`、`Evidence`、`AgentEvent`、`AgentRunResult` |
| 工作流节点 | [agents/runtime.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/runtime.py) | `query_router`、`answer`、`evidence_check`、`output_boundary` |
| 运行与 SSE | [agents/chat_service.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/chat_service.py) | `stream_chat`、`stream_run`、取消、重放、结果组装 |
| 工具边界 | [agents/tools.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/tools.py) | 工具白名单、BM25/重排、Neo4j 关系查询、降级 |
| 文本模型 | [integrations/model_gateway.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/model_gateway.py) | OpenAI-compatible `ChatOpenAI`、结构化输出、Token/成本 |
| 视觉模型 | [integrations/vision.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/vision.py) | 图片请求、`VisionObservation`、429/5xx 重试 |
| BM25 检索 | [integrations/retrieval.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/retrieval.py) | `Bm25Retriever.search`、字段权重、作物过滤、ES REST 客户端 |
| Embedding/重排 | [integrations/semantic.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/semantic.py) | `EmbeddingGateway`、`RerankerGateway`、失败回退 |
| 农业领域服务 | [domain/agriculture.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/domain/agriculture.py) | 实体目录、症状反查、图谱探索、路径、图片映射 |
| 防治安全边界 | [domain/control.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/domain/control.py) | 防治选项抽取、剂量表达检测、回答过滤 |
| SQLite 仓储 | [repositories/base.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/repositories/base.py)、[repositories/chats.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/repositories/chats.py)、[repositories/agent_runs.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/repositories/agent_runs.py) | WAL/锁、会话消息、Run/Step/Event/ToolCall 审计 |
| 分层记忆 | [repositories/memory.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/repositories/memory.py) | Redis TTL、PostgreSQL 版本记忆、ES KNN 副本 |
| 认证安全 | [core/security.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/core/security.py) | bcrypt、JWT、刷新、撤销、密码恢复 |
| API 路由 | [api/routers/chat.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/routers/chat.py)、[api/routers/diagnosis.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/routers/diagnosis.py)、[api/routers/agriculture.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/routers/agriculture.py) | 聊天、图片诊断、知识库和图谱 API |
| 前端入口与路由 | [frontend/src/main.ts](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/main.ts)、[frontend/src/router/routes/index.ts](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/router/routes/index.ts) | Vue 启动、5 个业务路由、基础布局 |
| 问答页面 | [frontend/src/views/chat/index.vue](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/views/chat/index.vue)、[frontend/src/utils/sse.ts](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/utils/sse.ts) | 会话管理、POST SSE、取消、流式渲染 |
| 诊断与知识页面 | [frontend/src/views/diagnosis/index.vue](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/views/diagnosis/index.vue)、[frontend/src/views/knowledge-base/index.vue](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/views/knowledge-base/index.vue)、[frontend/src/views/knowledge-graph/index.vue](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/views/knowledge-graph/index.vue) | 图片诊断、实体目录、图谱交互 |
| 评测公式 | [evaluation/metrics.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/evaluation/metrics.py) | 检索、Claim/引用、图片、防治关系、门禁公式 |
| 评测执行 | [run-rag-evaluation.py](F:/code/homework/project/agrigraph-evidence-qa/scripts/evaluation/run-rag-evaluation.py) | 三组真实评测、逐样例结果、候选摘要 |
| 数据准备 | [prepare-knowledge-data.py](F:/code/homework/project/agrigraph-evidence-qa/scripts/data/prepare-knowledge-data.py)、[import_knowledge.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/cli/import_knowledge.py) | 原始数据只读解析、统一 JSONL、ES Bulk 导入 |
| 图谱构建 | [build-neo4j-import.py](F:/code/homework/project/agrigraph-evidence-qa/scripts/graph/build-neo4j-import.py)、[import-agrigraph.cypher](F:/code/homework/project/agrigraph-evidence-qa/scripts/graph/import-agrigraph.cypher) | CSV 生成、实体/关系归一、Neo4j `MERGE` |
| 自动化测试 | [tests](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/conftest.py)、[test_metrics.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/evaluation/test_metrics.py)、[test_evidence_check.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/workflow/test_evidence_check.py) | 隔离数据库、指标公式、证据核验、安全边界 |
| 运行部署 | [compose.yml](F:/code/homework/project/agrigraph-evidence-qa/compose.yml)、[start-windows.bat](F:/code/homework/project/agrigraph-evidence-qa/start-windows.bat)、[runbook.md](F:/code/homework/project/agrigraph-evidence-qa/docs/runbook.md) | 容器依赖、AiJava 启动、迁移、健康检查 |
| 结果证据 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json)、[数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json)、[模型预检](F:/code/homework/project/agrigraph-evidence-qa/var/reports/model-preflight.json) | 已验收模型、样本规模、指标、源码/数据哈希 |

> 证据标记约定：**源码已确认**表示存在直接实现；**报告已确认**表示有结果文件；**根据接口推断**表示由调用关系得出；**用户材料**表示来自本次提供的简历文本；**待验证**表示代码已写入但缺少当前版本测试或运行报告。

## 1. 项目概述

### 1.1 项目类型

本项目是以下类型的组合：

| 类型 | 判断依据 |
| --- | --- |
| 全栈 Web 应用 | FastAPI 后端、Vue 3 前端、JWT 登录、HTTP/SSE 接口和业务页面同时存在。 |
| AI/Agent 应用 | 使用 LangGraph 固定工作流、文本/视觉模型网关、结构化输出、工具边界、运行审计。 |
| GraphRAG 知识应用 | Elasticsearch 召回农业实体，Neo4j 扩展显式关系，模型只基于检索证据回答。 |
| 多模态知识服务 | 图片先转换为作物、部位和可见症状，再复用文本检索与图谱链路。 |
| 非模型训练项目 | 仓库没有训练脚本、优化器、权重文件或训练日志；模型均通过兼容 API 调用。 |

### 1.2 当前实现状态

| 能力 | 状态 | 证据 |
| --- | --- | --- |
| FastAPI、固定 LangGraph、BM25、Neo4j、图片观察、Evidence Check、输出过滤、SSE | 已实现且存在 2026-08-30 候选验收 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |
| DeepSeek V4 Flash 与 Qwen3-VL-Flash 真实预检 | 已验证 | [模型预检](F:/code/homework/project/agrigraph-evidence-qa/var/reports/model-preflight.json) |
| BGE-Reranker 条件重排 | 源码已实现，待验证 | [semantic.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/semantic.py)、[tools.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/tools.py) |
| Redis + PostgreSQL + BGE-M3 + ES 分层记忆 | 源码已实现，待验证 | [memory.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/repositories/memory.py) |
| Qwen2.5/Qwen2.5-VL | 仅见于用户简历材料，当前源码默认配置和验收报告不支持该表述 | 当前配置为 DeepSeek/Qwen3-VL |

当前目录没有 `.git`，因此无法从提交历史还原 2025.08-2025.12 当时的准确源码版本。本文描述的是 **2026-09-01 当前工作区**，不是历史版本证明。

8 月 31 日新增能力统一标记为 `IMPLEMENTED_NOT_VERIFIED`：代码已写入，但尚未安装新增依赖、配置 BGE/记忆服务或重新运行测试和评测。

## 2. 项目背景与业务流程

### 2.1 背景与目标

- **用户材料：** 项目属于省重点研发项目，目标是建设农业知识服务平台。
- **源码已确认：** 当前实现只支持番茄和水稻，核心问题类型包括病害实体、症状、病原、发生条件、防治和关系查询。
- **根据接口推断：** 使用角色包括需要快速查询病虫害知识的普通用户，以及需要查看实体关系、证据和运行轨迹的农技/研究人员。
- **核心目标：** 把分散的农业文档、图片和实体关系整理为可检索、可解释、带引用并受安全边界约束的问答结果。

### 2.2 主要能力

1. 病害名称问答与田间症状反查。
2. 图片可见症状提取与候选病虫害召回。
3. 病害、症状、病原、发生条件和防治关系查询。
4. 文档证据、图谱关系、Claim 和引用展示。
5. 会话管理、运行轨迹、取消与重放。
6. 文本、图片和防治关系的冻结评测。

### 2.3 完整业务闭环

```text
公开农业数据（源码外只读）
  -> 清洗为统一农业实体 JSONL
  -> 导入 Elasticsearch 文档索引
  -> 构建并导入 Neo4j 实体关系
  -> 用户提交文本或图片
  -> LangGraph 固定工作流执行检索、生成、核验与过滤
  -> FastAPI/SSE 返回答案、候选实体、引用和运行状态
  -> SQLite 保存会话与运行审计
  -> 冻结评测集验证检索、证据、图片和防治边界
  -> 候选报告绑定数据、模型、Prompt、工作流和源码清单哈希
```

## 3. 个人工作

仓库没有 Git 提交历史或作者归属，无法仅凭源码证明每个模块由谁完成。因此需要将项目整体能力与个人职责分开。

### 3.1 可以确认的材料

- **用户材料：** 个人参与省重点研发项目，并计划在简历中描述问答编排、GraphRAG、多模态、分层记忆和评测。
- **源码已确认：** 这些模块在当前工作区均存在相应代码。
- **不能确认：** 仓库不能证明个人独立完成、主导架构、负责算法训练或负责全部前后端。

### 3.2 简历 Claim 与证据边界

| 候选表述 | 源码支持 | 运行证据 | 边界 |
| --- | --- | --- | --- |
| 基于 LangGraph 编排问答流程 | 支持 | 旧基线已验收 | 是固定工作流，不是 Supervisor 多 Agent。 |
| BM25 + Reranker + Neo4j GraphRAG | 源码支持 | BM25/Neo4j 已验收；Reranker 待验证 | 重排只用于症状/条件问题。 |
| 多模态辅助诊断 | 支持 | Qwen3-VL 基线已验收 | 只提取可见症状并召回候选，不直接确诊。 |
| Redis/PostgreSQL/ES 分层记忆 | 源码支持 | 待验证 | 记忆只帮助理解追问，不作为事实证据。 |
| Recall@10 95.1%、引用准确率 89.1% | 报告支持 | 已确认 | 属于 2026-08-30 BM25/DeepSeek/Qwen3-VL 基线，不覆盖新增 BGE/记忆。 |
| 使用 Qwen2.5/Qwen2.5-VL | 当前源码报告不支持 | 缺失 | 需要另行提供历史部署或运行证据。 |

## 4. 主要功能

### 4.1 农业知识问答

- 创建、选择、重命名和删除会话。
- 按作物范围限制查询。
- 结构化识别作物、任务类型、病害名、症状、部位和目标。
- 返回答案、候选实体、Claim、引用、图谱关系、防治选项、告警、延迟和 Token 用量。

### 4.2 图片辅助诊断

- 支持 JPEG、PNG、WebP，单文件上限 10 MB。
- 视觉模型只提取作物、部位、症状和质量提示。
- 将视觉观察转换为 `AgricultureQuery`，复用同一 GraphRAG 工作流。
- 图片不可用时前端显示未配置；模型异常由接口返回错误，文本问答能力保持独立。

### 4.3 知识库与图谱

- 农业实体列表、详情、图片、相似实体和对比。
- 症状候选反查。
- 图谱建议、概览、邻域、路径和相关实体查询。
- 受控读取数据根目录下的本地图片。

### 4.4 运行治理

- Run、Step、Event、ToolCall 审计。
- 运行状态筛选、详情查询和增量事件读取。
- 协作式取消与按原目标重放。
- 模型调用和工具调用计数、延迟、Token 与可选成本估算。

## 5. 总体架构

### 5.1 文字架构图

```text
Vue 3 页面
  -> Axios JSON API / fetch POST SSE
  -> FastAPI Router + JWT Principal
  -> AppContainer
      -> SQLite RepositoryHub（用户、会话、运行审计）
      -> LayeredMemory（Redis/PostgreSQL/ES，可选）
      -> LangGraph StateGraph
          session_context
          -> query_router
          -> document_research（ES BM25，可选 BGE 重排）
          -> graph_reasoning（Neo4j）
          -> answer（文本模型）
          -> evidence_check
          -> output_boundary
          -> finalize
      -> SSE / JSON Response
```

图片链路在进入图之前调用视觉模型生成 `VisionObservation`；图内不保存图片字节。

### 5.2 模块依赖

- `api` 只依赖 `AppContainer` 暴露的服务。
- `core/container.py` 是外部服务和业务对象的唯一装配点。
- `agents` 依赖领域 Schema、工具注册、模型网关和仓储接口。
- `integrations` 封装模型、ES 和 Neo4j 调用。
- `repositories` 负责 SQLite 业务事实与可选分层记忆。
- `evaluation` 不依赖在线模型，可独立复算指标。

### 5.3 同步与异步边界

- FastAPI 多数业务接口是同步函数；图片模型调用通过 `run_in_threadpool` 避免阻塞事件循环。
- SSE 使用同步生成器逐步产出公开事件，不是 WebSocket。
- 批量检索评测使用 `ThreadPoolExecutor(max_workers<=4)`。
- 没有后台 Worker、消息队列或分布式任务调度。

### 5.4 运行模式

- **本地/验收：** Conda `AiJava` + Docker Compose 依赖 + Vite 前端。
- **模型未配置：** 文本节点使用确定性降级；视觉入口返回未配置；BGE/长期语义记忆关闭。
- **已验收基线：** DeepSeek + Qwen3-VL + ES BM25 + Neo4j + SQLite。
- **新增未验证模式：** 条件 BGE 重排 + Redis/PostgreSQL/ES 分层记忆。

## 6. 技术栈与实现

### FastAPI + Pydantic

- 所属层次：后端 API 与输入输出合同。
- 用在什么位置：应用创建、Router、依赖注入、上传和 Schema 校验。
- 实际能力：REST API、SSE Response、统一异常、CORS、`BaseModel`/`TypedDict` 结构化状态。
- 输入：JSON、Multipart 图片、Bearer Token、查询参数。
- 输出：统一 `{code,message,data}`、SSE 事件或文件响应。
- 解决的问题：隔离 HTTP 协议与业务工作流，保证模型输出进入系统前经过 Schema 校验。
- 关键状态：`ChatRequest`、`AgricultureQuery`、`VisionObservation`、`AgentRunResult`。
- 异常处理：LookupError -> 404，ValueError -> 400；外部异常按各网关策略处理。
- 选择原因：根据源码结构推断，FastAPI 与 Pydantic 适合类型化 API 和模型结构化输出。
- 候选取舍：未使用 Django/Flask；仓库没有相关依赖。
- 对应源码：[main.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/main.py)、[api/schemas.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/schemas.py)、[domain/models.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/domain/models.py)。
- 对应验证：[test_core_api.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/api/test_core_api.py)。

### LangGraph

- 所属层次：AI 工作流编排。
- 用在什么位置：固定农业问答图和 SQLite Checkpoint。
- 实际能力：`StateGraph`、节点状态合并、流式状态产出、Checkpointer。
- 输入：一次运行的 `AgentState`。
- 输出：逐节点增量状态和最终 `AgentRunResult`。
- 解决的问题：将问题理解、检索、生成、核验和过滤拆成可审计阶段。
- 关键状态：查询、证据、候选、Claim、引用、事件、调用计数。
- 异常处理：节点工具错误转为空结果和 warning；全局超时或取消使 Run 失败/取消。
- 选择原因：显式图状态便于分阶段审计和 SSE 展示。
- 候选取舍：未使用 Supervisor/ReAct 动态循环；固定流程减少工具越权和不可控循环。
- 对应源码：[chat_graph.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/chat_graph.py)、[runtime.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/runtime.py)。
- 对应验证：[test_evidence_check.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/workflow/test_evidence_check.py)；当前没有完整图顺序测试。

### 文本大模型兼容网关

- 所属层次：问题结构化与证据回答生成。
- 用在什么位置：`query_router` 和 `answer`。
- 实际能力：LangChain `ChatOpenAI`、温度 0、Pydantic 结构化输出、一次重试、Token/成本提取。
- 输入：系统约束、用户问题、文档/图谱证据摘要。
- 输出：`AgricultureQuery` 或 `AnswerDraft`。
- 解决的问题：把非结构化问题转换为检索条件，并把证据组织为带 Claim/引用的回答。
- 关键状态：模型 ID、Prompt SHA-256、输入/输出 Token、可选成本。
- 异常处理：模型异常返回 `None`，工作流使用确定性查询或回答降级。
- 选择原因：兼容 API 可替换供应商而不改变业务节点。
- 候选取舍：未训练农业基础模型；当前报告验证的是 DeepSeek V4 Flash。
- 对应源码：[model_gateway.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/model_gateway.py)。
- 对应验证：[test_model_gateway.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/integrations/test_model_gateway.py)、[模型预检](F:/code/homework/project/agrigraph-evidence-qa/var/reports/model-preflight.json)。

### 视觉模型兼容网关

- 所属层次：图片可见特征理解。
- 用在什么位置：`POST /diagnosis/image`。
- 实际能力：OpenAI-compatible 图片消息、Base64 Data URL、结构化 JSON、429/5xx/Transport 重试。
- 输入：临时图片和作物提示。
- 输出：作物、部位、症状、图片质量提示。
- 解决的问题：将图片转换为可复用的农业文本查询，不让视觉模型直接确诊。
- 关键状态：`VisionObservation`。
- 异常处理：最多重试一次；未配置返回 503；临时文件最终删除。
- 选择原因：与文本 GraphRAG 链路解耦，视觉只承担观察。
- 候选取舍：当前报告验证 Qwen3-VL-Flash；简历中的 Qwen2.5-VL 缺少当前证据。
- 对应源码：[vision.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/vision.py)、[diagnosis.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/routers/diagnosis.py)。
- 对应验证：[test_vision.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/integrations/test_vision.py)、[模型预检](F:/code/homework/project/agrigraph-evidence-qa/var/reports/model-preflight.json)。

### Elasticsearch + BM25

- 所属层次：农业知识召回与长期记忆检索副本。
- 用在什么位置：实体索引、目录/搜索 API、工作流文档检索、可选记忆 KNN。
- 实际能力：`multi_match` 字段加权、短语匹配、作物过滤、Bulk 导入、稳定文档 ID。
- 输入：结构化后的检索文本、作物范围、TopK。
- 输出：候选实体、文本证据、检索分数和排名元数据。
- 解决的问题：精确实体名和农业术语召回。
- 关键数据：`agrigraph_evidence_v2`；新增记忆索引默认 `agrigraph_memory_v1`。
- 异常处理：BM25 客户端异常返回空结果；记忆异常不阻断主问答。
- 选择原因：同一组件同时支持关键词检索、过滤和可选 dense_vector 记忆索引。
- 候选取舍：实体主索引没有向量字段；没有 Milvus/PGVector。
- 对应源码：[retrieval.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/retrieval.py)、[import_knowledge.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/cli/import_knowledge.py)。
- 对应验证：[test_import_knowledge.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/cli/test_import_knowledge.py)、[数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json)。

### BGE-M3 + BGE-Reranker

- 所属层次：可选语义向量与二阶段重排。
- 用在什么位置：长期记忆向量化；症状/发生条件查询候选重排。
- 实际能力：OpenAI-compatible `/embeddings`、常见 `/rerank` 返回兼容、维度校验、重排元数据。
- 输入：记忆文本，或查询与 BM25 候选文本。
- 输出：按当前配置校验的 1024 维向量，或 TopN 重排结果；实际外部服务返回仍待验证。
- 解决的问题：跨会话语义记忆召回和描述性症状候选排序。
- 关键状态：`rerankerModel`、`bm25Rank`、新 rank、degraded/reason。
- 异常处理：Reranker 失败保留 BM25 顺序；Embedding 失败跳过长期记忆。
- 选择原因：通过可选网关增强现有 BM25，不替换稳定基线。
- 候选取舍：没有把所有请求都送去重排；实体/病原/防治问题保留 BM25。
- 对应源码：[semantic.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/semantic.py)、[tools.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/tools.py)。
- 对应验证：**仓库没有当前实现的单元/集成测试或候选报告，状态为待验证。**

### Neo4j

- 所属层次：农业知识图谱与关系解释。
- 用在什么位置：关系扩展、图谱浏览、邻域、路径、相关实体、防治关系评测。
- 实际能力：Cypher、`MERGE` 导入、无向邻域查询、变长路径、关系类型过滤。
- 输入：候选实体 ID、病害名称或源/目标节点。
- 输出：关系证据、节点、边、路径和统计。
- 解决的问题：补充症状、病原、条件和防治的显式关联，提供可解释关系。
- 关键关系：`HAS_SYMPTOM`、`CAUSED_BY`、`FAVORED_BY`、`CONTROLS`、`PREVENTS`。
- 异常处理：领域服务图查询异常返回空记录；工具层记录降级。
- 选择原因：关系和路径查询比在文本文档中模拟图结构更直接。
- 候选取舍：不使用复杂图算法或社区检测；聚焦业务关系查询。
- 对应源码：[agriculture.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/domain/agriculture.py)、[build-neo4j-import.py](F:/code/homework/project/agrigraph-evidence-qa/scripts/graph/build-neo4j-import.py)。
- 对应验证：[数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json)、[防治关系评测集](F:/code/homework/project/agrigraph-evidence-qa/tests/evaluation/control-relation-test.jsonl)。

### SQLite + Alembic

- 所属层次：用户、会话、令牌、运行审计和 LangGraph Checkpoint。
- 用在什么位置：RepositoryHub 与 SqliteSaver。
- 实际能力：WAL、外键、busy timeout、短连接、进程内写锁、版本迁移。
- 输入：用户操作、消息、运行事件和工具摘要。
- 输出：会话历史、运行详情、撤销状态和恢复检查点。
- 解决的问题：单机求职展示版的低运维持久化。
- 关键表：users、token_sessions、chat_sessions、chat_messages、agent_runs、agent_steps、agent_events、agent_tool_calls。
- 异常处理：唯一约束转业务错误；连接设置 5 秒锁等待。
- 选择原因：项目运行规模和单机部署下足够简单；Checkpoint 与业务库分文件。
- 候选取舍：不把全部业务迁移到 PostgreSQL；后者仅用于新增长期记忆。
- 对应源码：[base.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/repositories/base.py)、[initial_schema.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/migrations/initial_schema.py)。
- 对应验证：[conftest.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/conftest.py)、[test_password_reset.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/tests/core/test_password_reset.py)。

### Redis + PostgreSQL/Psycopg

- 所属层次：可选分层记忆。
- 用在什么位置：Redis 短期消息，PostgreSQL 长期记忆和用户档案。
- 实际能力：Redis list/pipeline/TTL；PostgreSQL JSONB、版本号、状态、过期时间、冲突引用、事务。
- 输入：真实聊天会话消息、用户问题和候选实体。
- 输出：有界短期上下文、跨会话相关记忆和用户档案。
- 解决的问题：限制 Prompt 历史长度，同时允许跨会话恢复相关背景。
- 关键状态：ACTIVE/SUPERSEDED/DELETED、version、conflict_of、expires_at。
- 异常处理：外部存储失败被隔离，不阻断主问答；SQLite 最近消息作为短期回退。
- 选择原因：热上下文与长期事实分工；PostgreSQL 作为长期权威记录，ES 只保存检索副本。
- 候选取舍：没有引入独立 Memory Agent 或专用向量数据库。
- 对应源码：[memory.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/repositories/memory.py)。
- 对应验证：**当前没有 Redis/PostgreSQL/ES 记忆集成测试，状态为待验证。**

### Vue 3 + TypeScript + Vite + Pinia + Naive UI

- 所属层次：Web 前端。
- 用在什么位置：问答、知识库、图谱、诊断、运行轨迹和登录页面。
- 实际能力：静态路由、Pinia 鉴权状态、Axios 请求、Markdown 渲染、ECharts 图谱、Vite 构建。
- 输入：用户表单、图片、查询和会话操作。
- 输出：流式答案、候选实体、引用、图谱和运行轨迹。
- 解决的问题：将模型和检索过程呈现为可操作的农业知识应用。
- 关键状态：Token、会话列表、消息列表、当前请求 ID、AbortController。
- 异常处理：请求错误提示、Token 刷新、SSE 中断和取消。
- 选择原因：现有 Soybean Admin 基础提供布局、主题和路由生成能力。
- 候选取舍：没有 SSR；前端不是模型推理层。
- 对应源码：[chat/index.vue](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/views/chat/index.vue)、[request/index.ts](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/service/request/index.ts)。
- 对应验证：仓库提供 `pnpm typecheck` 和 `pnpm build`，没有浏览器 E2E 测试。

### HTTP SSE

- 所属层次：后端到前端的流式传输。
- 用在什么位置：聊天消息流。
- 实际能力：POST 请求体、Bearer Token、`event/data` 帧、网络分片缓冲、AbortSignal。
- 输入：问题、作物范围、requestId。
- 输出：connected、workflow、retrieval_result、answer_chunk、complete、error、message_saved。
- 解决的问题：在模型完成前反馈工作流进度和答案片段。
- 关键状态：runId/requestId 和公开事件。
- 异常处理：HTTP 状态检查、流分片缓冲、前端 abort、后端协作式取消。
- 选择原因：单向服务端推送适合问答进度，不需要 WebSocket 双向协议。
- 候选取舍：前端虽依赖 `ws`，当前业务代码没有使用 WebSocket。
- 对应源码：[responses.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/responses.py)、[sse.ts](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/utils/sse.ts)。
- 对应验证：当前缺少前后端 SSE 合同测试，且已发现字段漂移风险。

### Docker Compose + Nginx

- 所属层次：本地依赖与静态部署辅助。
- 用在什么位置：ES、Neo4j、PostgreSQL、Redis 容器和前端反向代理示例。
- 实际能力：独立端口、数据卷、健康检查、密码环境变量。
- 输入：被忽略的 `infra.env` 和本地端口配置。
- 输出：可供后端连接的基础服务。
- 解决的问题：隔离项目依赖与旧项目数据卷。
- 关键状态：Compose 项目名 `agrigraph-evidence-v2`。
- 异常处理：`up --wait`、健康检查、启动脚本失败退出。
- 选择原因：适合单机复现；仓库没有集群编排需求。
- 候选取舍：没有 Kubernetes/CI/CD；Nginx 示例端口仍需修正。
- 对应源码：[compose.yml](F:/code/homework/project/agrigraph-evidence-qa/compose.yml)、[agrigraph.conf](F:/code/homework/project/agrigraph-evidence-qa/deploy/nginx/agrigraph.conf)。
- 对应验证：历史记录显示 Compose 配置曾通过；新增 PostgreSQL/Redis 后未重新验收。

## 7. 核心调用链

### 7.1 文本问答与 SSE

```text
Chat 页面 sendQuestion
  -> postSse 携带 Bearer Token 和 ChatRequest
  -> chat.ask_stream 校验用户与会话
  -> AgentService.stream_chat 保存用户消息
  -> AgentService.stream_run 创建 agent_run
  -> MainAgentGraph.stream
  -> session_context 加载短期/长期上下文
  -> query_router 生成 AgricultureQuery
  -> document_research 执行 BM25/可选重排
  -> graph_reasoning 查询 Neo4j
  -> answer 生成 AnswerDraft
  -> evidence_check 删除无证据 Claim 并重组答案
  -> output_boundary 过滤农药使用参数
  -> agent_runs/steps/events/tool_calls 持久化
  -> SSE 返回事件与最终结果
  -> chat_messages 保存助手回答
```

关键文件：[chat/index.vue](F:/code/homework/project/agrigraph-evidence-qa/frontend/src/views/chat/index.vue)、[chat.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/routers/chat.py)、[chat_service.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/agents/chat_service.py)。

### 7.2 图片辅助诊断

```text
Diagnosis 页面上传图片
  -> diagnosis.diagnose 校验登录、类型和 10 MB 大小
  -> 临时保存图片
  -> VisionGateway.analyze 输出 VisionObservation
  -> 转换为 ChatRequest + AgricultureQuery
  -> 复用固定 GraphRAG 工作流
  -> 可选按已核验图片 SHA-256 补充候选
  -> 返回 observation、candidateEntities、answer、citations
  -> finally 删除临时图片
```

关键文件：[diagnosis.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/api/routers/diagnosis.py)、[vision.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/integrations/vision.py)。

### 7.3 分层记忆

```text
真实会话保存用户/助手消息
  -> Redis RPUSH + LTRIM + EXPIRE
  -> 回答完成后 BGE-M3 生成记忆向量
  -> PostgreSQL 事务写入版本化 memory/profile
  -> 旧版本标记 SUPERSEDED
  -> ES 写入向量检索副本

下一次提问
  -> Redis 最近消息 + ES KNN 长期记忆
  -> owner/project/status/expiresAt 过滤
  -> 最多 2 条长期记忆 + 短期消息
  -> query_router 仅用于指代补全
```

注意：PostgreSQL 与 ES 是顺序双写，没有 Outbox 或补偿任务；若 PG 成功而 ES 失败，会留下暂不可检索的长期记录。

### 7.4 数据准备与导入

```text
AGRIGRAPH_DATA_ROOT/raw
  -> prepare-knowledge-data.py 读取表格/HTML/OWL/图片清单
  -> processed/knowledge/*.jsonl + 质量报告
  -> app.import_knowledge 清洗防治字段并 Bulk 导入 ES
  -> build-neo4j-import.py 生成 nodes.csv/edges.csv
  -> import-agrigraph.cypher 使用 MERGE 幂等导入 Neo4j
  -> verify_data_baseline 校验数量、关系和敏感字段
```

### 7.5 认证链路

```text
登录用户名/密码
  -> bcrypt 校验
  -> 生成 access + refresh JWT
  -> token_sessions 保存 tokenId/过期时间
  -> 前端每次请求注入 Bearer Token
  -> Authenticator 校验签名、类型、撤销状态
  -> 401 时前端串行刷新 Token 并重放请求
```

## 8. 数据、状态和并发

### 8.1 数据权威来源

| 数据 | 权威来源 | 派生副本 |
| --- | --- | --- |
| 原始农业资料和图片 | `AGRIGRAPH_DATA_ROOT/raw` | processed JSONL、ES、Neo4j |
| 用户、会话、Token、运行审计 | SQLite | 无 |
| 长期记忆事实 | PostgreSQL（新增） | Elasticsearch 记忆向量索引 |
| 短期上下文 | Redis（新增） | SQLite 会话作为降级来源 |
| LangGraph Checkpoint | 独立 SQLite 文件 | 无 |

### 8.2 核心实体和关系

- `AgricultureQuery`：作物、任务类型、病害名、症状、部位、查询目标。
- `Evidence`：document/graph/memory、标题、摘录、分数和元数据。
- `AnswerClaim`：文本与 evidenceIds。
- 图谱节点：Disease、Symptom、Pathogen、EnvironmentFactor、ActiveIngredient、BiologicalControl、AgriculturalPractice 等。
- 图谱关系：HAS_SYMPTOM、CAUSED_BY、FAVORED_BY、CONTROLS、PREVENTS。

### 8.3 事务、一致性与幂等

- SQLite 单次 `with connection` 提交事务，写入受 `RLock` 保护。
- PostgreSQL 记忆的版本替换、插入和用户档案更新处于同一事务。
- PostgreSQL 与 ES 记忆索引不是原子事务，当前无 Outbox/补偿。
- ES 实体导入使用稳定 `_id`，重复执行覆盖同一实体。
- Neo4j 导入使用 `MERGE`，支持幂等执行。
- 会话删除先删 SQLite，再尝试删除 Redis/长期记忆；外部记忆删除失败被吞掉，可能产生残留。

### 8.4 并发、重试、超时和取消

- SQLite：WAL + busy timeout 5 秒 + 进程内 RLock。
- 模型：文本模型 `max_retries=1`；视觉 429/5xx 最多重试一次。
- 工具：异常降级，不做自动多次重试。
- 工作流：总超时默认 300 秒；模型默认 90 秒；视觉默认 120 秒。
- 评测：文本并发最多 4，视觉并发最多 2。
- 取消：前端 abort + 后端 `CANCEL_REQUESTED` 状态检查，属于协作式取消。
- 恢复：LangGraph Checkpoint 存在，但当前重放接口是按原目标重新执行，不是从任意失败节点精确恢复。

## 9. 接口与外部集成

### 9.1 主要 API

除健康检查和图片媒体外，业务接口默认要求 Bearer JWT。

| 分组 | 主要入口 | 输入 | 输出 |
| --- | --- | --- | --- |
| 认证 | `POST /api/v1/users/login`、`POST /auth/refreshToken` | 用户名密码/刷新令牌 | access + refresh Token |
| 会话 | `/api/v1/agriculture-chat/sessions` | 标题、作物范围 | 会话与消息 |
| 流式问答 | `POST /sessions/{id}/messages/stream` | `ChatRequest` | SSE 事件流 |
| 图片诊断 | `POST /api/v1/diagnosis/image` | Multipart 图片、crop | observation + 候选 + 回答 |
| 实体目录 | `/api/v1/agriculture/entities` | 作物、类别、部位、关键词、分页 | 实体列表/详情 |
| 图谱 | `/api/v1/graph/*` | 节点、关系、路径参数 | 节点、边、路径、统计 |
| 运行审计 | `/api/v1/agent-runs/*` | 状态、runId、after | Run/Step/Event/ToolCall |
| 评测 | `/api/v1/evaluation/*` | 问题列表、实体 ID | 检索/回答/防治关系结果 |
| 健康 | `/api/v1/health`、`/health/dependencies` | 无 | 应用和依赖状态 |

### 9.2 统一合同

- 普通响应：`{code, message, data}`。
- SSE：`event: <type>\ndata: <json>\n\n`。
- 认证：`Authorization: Bearer <token>`。
- 模型：OpenAI-compatible Chat/Embedding/Rerank 变体。
- 图数据库：Neo4j Python Driver + Cypher。
- ES：HTTP JSON REST。

### 9.3 外部服务与失败策略

| 服务 | 超时/重试 | 失败行为 |
| --- | --- | --- |
| 文本模型 | 90 秒，SDK 1 次重试 | 确定性查询/回答降级 |
| 视觉模型 | 120 秒，429/5xx/传输错误重试 1 次 | 图片接口失败，文本问答仍可用 |
| Elasticsearch BM25 | 5 秒 | 返回空候选 |
| BGE Reranker | 30 秒 | 保留 BM25 顺序并标记降级 |
| BGE Embedding | 30 秒 | 跳过长期语义记忆 |
| Neo4j | 建连 5 秒，不做事务重试 | 空关系或工具降级 |
| PostgreSQL/Redis 记忆 | 2-3 秒 | 不阻断主问答，SQLite 短期回退 |

## 10. 工程质量

### 10.1 配置与 Secret

- `.env.ai.example` 只提供键名和空值；真实 `.env.ai` 被忽略且本文不读取。
- `infra.env` 由启动脚本生成本地数据库/JWT 密钥。
- API Key、密码和 JWT 不应进入报告。
- **已发现问题：** 前端请求层存在硬编码的额外 Token 头，应删除或环境化；本文不展示其值。

### 10.2 迁移与数据初始化

- 应用创建 RepositoryHub 时自动执行 Alembic upgrade。
- 启动脚本也在 Uvicorn 前显式执行 `alembic upgrade head`。
- PostgreSQL 记忆表目前由代码 `CREATE TABLE IF NOT EXISTS` 懒初始化，不属于 Alembic 迁移。

### 10.3 日志、Trace、Metrics

- 有 SQLite Run/Step/Event/ToolCall 审计和前端时间线。
- 有依赖健康检查、节点耗时、模型 Token、可选成本。
- 没有 OpenTelemetry、Prometheus、Jaeger 或统一结构化日志框架。
- 本地进程日志写入 `logs/`，查看脚本负责 UTF-8 展示。

### 10.4 自动化测试和静态检查

- 当前源码树可静态识别 27 个 `test_*` 函数。
- 测试覆盖认证/密码恢复、工具白名单、BM25 映射、防治过滤、Evidence Check、视觉网关、冻结数据和指标公式。
- **当前缺口：** BGE、PostgreSQL/Redis 记忆、ES KNN、条件重排和 SSE 合同没有测试。
- **测试漂移：** `test_tools.py` 未传 `session.load` 新增的 `project_scope` 参数。

命令：

```powershell
Push-Location backend-python
conda run --no-capture-output -n AiJava python -m ruff check app tests migrations
conda run --no-capture-output -n AiJava python -m pytest tests
conda run --no-capture-output -n AiJava python -m alembic upgrade head
Pop-Location

Push-Location frontend
pnpm typecheck
pnpm build
Pop-Location
```

### 10.5 CI/CD 与部署

- 仓库没有 GitHub Actions、GitLab CI、Jenkins 或 Azure Pipeline。
- Compose、Windows 启停脚本和自动验收脚本属于本地交付工具，不等同于 CI/CD。
- Nginx 示例仍代理 8088，与当前后端 8188 不一致，使用前需修改。

### 10.6 当前有效与历史残留

| 类型 | 内容 |
| --- | --- |
| 当前业务主线 | chat、diagnosis、knowledge-base、knowledge-graph、agent-runs；FastAPI 对应 Router。 |
| 前端基础模板 | Soybean Admin 布局、主题、搜索、标签页和通用组件。 |
| 历史/未接入 | 分片上传 Store 的 `/upload/chunk`、`/upload/merge`；组织标签类型；旧 Hybrid 工作流标签。 |
| 合同漂移 | 前端 `complete/workflow` 事件仍按旧 `data.data/state` 形态读取；后端发送当前公开结构。 |
| 命名漂移 | SQLite `supervisor_id` 遗留值不代表当前存在 Supervisor；健康响应仍固定 `retrievalMode=bm25`。 |

## 11. 评估指标

### 11.1 数据与模型范围

- Test V2：160 条，其中 144 条有答案正样本、16 条负样本。
- 生成子集：40 条，其中候选摘要记录 32 条正样本、8 条负样本。
- 图片：30 张，番茄/水稻各 15 张。
- 防治关系：30 条，来源审核口径为 `SOURCE_CURATED_PUBLIC_DATA`。
- 已验收模型：`deepseek-v4-flash`、`qwen3-vl-flash`。
- 报告状态：`COMPLETED_CANDIDATE`，但同时标记 `CANDIDATE_NOT_FORMAL_RELEASE`、`UNVERSIONED_LOCAL`。

### 11.2 指标结果

| 评估对象 | 数据规模 | 基线 | 当前方案 | 指标 | 结果 | 证据文件 |
| --- | ---: | --- | --- | --- | ---: | --- |
| 文本候选检索 | 144 正样本 | 未记录独立对照 | Elasticsearch BM25 Top10 | Recall@10 | 0.9514 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |
| 文本候选排序 | 144 正样本 | 未记录独立对照 | Elasticsearch BM25 | MRR / nDCG@10 | 0.8217 / 0.8533 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |
| 生成事实覆盖 | 32 正样本 | 未记录独立对照 | DeepSeek 证据回答 | Claim Coverage | 0.8229 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |
| 回答引用 | 32 正样本 | 未记录独立对照 | Claim/引用核验 | Citation Coverage / Correctness | 0.9062 / 0.8906 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |
| 负样本拒答 | 8 负样本 | 未记录独立对照 | 证据不足拒答 | Negative Refusal Rate | 0.8750 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |
| 图片理解与候选 | 30 张 | 未记录独立对照 | Qwen3-VL + GraphRAG | Schema / 症状覆盖 / Recall@3 | 1.0000 / 0.1278 / 1.0000 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |
| 防治关系 | 30 条 | 未记录独立对照 | ES 选项 + Neo4j 关系 | Recall@5 / 正确率 / 泄漏率 | 1.0000 / 1.0000 / 0.0000 | [候选摘要](F:/code/homework/project/agrigraph-evidence-qa/var/reports/multimodal-candidate-summary.json) |

指标定义见 [metrics.py](F:/code/homework/project/agrigraph-evidence-qa/backend-python/app/evaluation/metrics.py)。报告没有 BGE-Reranker 前后对比，因此不能声称重排提升了 Recall/MRR；也不能把上述指标归因于 Qwen2.5。

### 11.3 数据基线

| 对象 | 结果 | 证据 |
| --- | ---: | --- |
| ES 农业实体 | 158 | [数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json) |
| Neo4j 节点 | 1749 | [数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json) |
| Neo4j 关系 | 3312 | [数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json) |
| HAS_SYMPTOM / CAUSED_BY / FAVORED_BY | 548 / 95 / 230 | [数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json) |
| CONTROLS / PREVENTS | 154 / 118 | [数据基线](F:/code/homework/project/agrigraph-evidence-qa/var/reports/data-baseline-verification.json) |

### 11.4 工程诊断

候选摘要记录 P50 13933.5 ms、P95 43363.85 ms、平均 Token 7233.34；成本为 null，因为未配置单价。`elapsedSeconds=0.52` 是摘要恢复/生成阶段，不能解释为完整真实评测耗时。

## 12. 测试与验收

### 12.1 已有验证层次

1. Ruff 静态检查。
2. Pytest 单元与 API 合同测试。
3. 空 SQLite Alembic 升级。
4. Vue TypeScript 类型检查和生产构建。
5. Compose 依赖健康检查。
6. ES/Neo4j 数据基线验证。
7. 文本、图片、防治关系真实评测。
8. 候选门禁：样本完整、模型预检、依赖正常、指标达标后才写 `COMPLETED_CANDIDATE`。

### 12.2 当前验收边界

- 2026-08-30 的候选报告可以证明旧基线。
- 2026-08-31 新增 BGE/记忆后没有重新运行质量门禁。
- 当前不能声称“所有 27 项测试通过最新源码”，只能说仓库有 27 个测试函数，历史进度记录曾在新增扩展前通过。
- 当前没有浏览器 E2E、并发压测、Reranker 对比或记忆一致性验收。

## 13. 运行与部署

### 13.1 环境要求

- Conda `AiJava`，Python 3.10.19。
- Node.js >= 18.20，pnpm >= 8.7。
- Docker Desktop。
- 源码外部农业数据目录，通过 `AGRIGRAPH_DATA_ROOT` 指定。

### 13.2 初始化与启动

```powershell
# 1. 启动 ES、Neo4j、PostgreSQL、Redis
powershell -ExecutionPolicy Bypass -File scripts/dev/start-acceptance-dependencies.ps1

# 2. 启动后端和前端
.\start-windows.bat
```

启动器会验证 Conda 环境、执行 Alembic，再启动 Uvicorn；前端由 Vite 启动。

### 13.3 数据准备与导入

```powershell
$env:AGRIGRAPH_DATA_ROOT = 'F:\Datasets\AgriGraph'
conda run -n AiJava python scripts/data/prepare-knowledge-data.py
conda run -n AiJava python -m app.import_knowledge --data-root $env:AGRIGRAPH_DATA_ROOT
conda run -n AiJava python scripts/graph/build-neo4j-import.py
```

Neo4j 正式导入应使用仓库已有 PowerShell/Cypher 脚本，详见 [runbook.md](F:/code/homework/project/agrigraph-evidence-qa/docs/runbook.md)。

### 13.4 访问地址

- 前端：`http://127.0.0.1:9627`
- API：`http://127.0.0.1:8188`
- 依赖健康：`http://127.0.0.1:8188/api/v1/health/dependencies`
- ES：`127.0.0.1:29200`
- Neo4j Browser/Bolt：`127.0.0.1:27474/27687`
- PostgreSQL：`127.0.0.1:25432`
- Redis：`127.0.0.1:26379`

### 13.5 常见故障入口

| 现象 | 排查入口 |
| --- | --- |
| 后端启动失败 | `logs/python-backend.log`、AiJava 校验、Alembic |
| 依赖降级 | `/api/v1/health/dependencies` |
| 图片入口 503 | VISION_API_BASE/KEY/MODEL 配置 |
| BGE 不生效 | Embedding/Reranker 端点为空或网关失败；查看检索 metadata |
| 无图谱关系 | Neo4j 密码、数据导入和实体 ID 对齐 |
| SSE 页面无最终元数据 | 检查前后端 complete/workflow 事件字段漂移 |
| 最新测试失败 | 先修复 `session.load` 的 `project_scope` 测试签名 |

## 14. 项目目录

```text
agrigraph-evidence-qa/
├─ backend-python/
│  ├─ app/
│  │  ├─ agents/          固定 LangGraph、状态、工具、SSE 服务
│  │  ├─ api/             FastAPI Router、Schema、统一响应
│  │  ├─ core/            配置、容器、认证、健康检查
│  │  ├─ domain/          农业实体、查询、图谱、防治规则
│  │  ├─ evaluation/      可独立复算的评测公式
│  │  ├─ integrations/    模型、视觉、ES、Neo4j、BGE 网关
│  │  └─ repositories/    SQLite 与分层记忆
│  ├─ migrations/         SQLite Alembic 迁移
│  └─ tests/              后端自动化测试
├─ frontend/
│  ├─ src/views/          5 个核心业务页面
│  ├─ src/service/        HTTP 请求与认证刷新
│  ├─ src/utils/sse.ts    POST SSE 解析
│  └─ packages/           模板基础包
├─ config/                数据集登记、本体、防治别名、Prompt
├─ scripts/
│  ├─ data/               数据下载、整理、许可审计
│  ├─ graph/              Neo4j CSV/Cypher 构建与导入
│  ├─ evaluation/         冻结集生成与真实评测
│  └─ dev/                启停、配置和自动验收
├─ tests/evaluation/      冻结评测 JSONL
├─ docs/                  架构、运行、评测和问题复盘
├─ deploy/nginx/          静态发布示例
├─ compose.yml            ES/Neo4j/PostgreSQL/Redis
└─ var/                   被忽略的数据库、报告和运行产物
```

## 15. 技术取舍与扩展

### 15.1 当前取舍

| 取舍 | 当前实现 | 优势 | 代价 |
| --- | --- | --- | --- |
| 固定 LangGraph vs 动态 Supervisor | 固定顺序节点 | 可控、易审计、避免循环 | 不能动态规划复杂任务 |
| ES + Neo4j | 文本召回与关系扩展分工 | 兼顾关键词和显式关系 | 双存储导入与 ID 一致性成本 |
| 视觉观察 vs 直接诊断 | 视觉只提取可见特征 | 降低过度诊断风险 | 仍依赖后续知识召回质量 |
| SQLite 主业务 + 可选分层记忆 | 单机业务事实保持简单 | 低运维、兼容旧数据 | 多存储一致性复杂度增加 |
| 可选 Reranker | 只用于症状/条件问题 | 控制延迟和依赖范围 | 缺少已验证的排序收益 |
| 双层防治过滤 | 导入清洗 + 输出过滤 | 降低剂量泄漏 | 规则可能误删合法描述 |

### 15.2 当前瓶颈与优先修复

1. 修复前后端 SSE 合同，增加事件契约测试。
2. 更新 `test_tools.py` 新签名并为 BGE、Redis、PostgreSQL、ES KNN 增加测试。
3. 配置真实 BGE 服务，比较 BM25 与重排后的 MRR/nDCG，再决定是否正式启用。
4. 为 PostgreSQL -> ES 记忆双写增加 Outbox/补偿或重建索引命令。
5. 清理前端旧上传 Store、组织标签类型和旧工作流标签。
6. 删除或环境化前端硬编码额外 Token 头。
7. 修正 Nginx 端口和历史验收文档。

### 15.3 规模扩大后的扩展顺序

以下是建议，不是当前能力：

1. 先把 SQLite 业务事实迁移到 PostgreSQL，并统一迁移体系。
2. 再为运行审计和模型调用增加结构化日志与 OpenTelemetry。
3. 当真实异步任务出现后再引入任务队列/Worker，不因关键词提前加入 Kafka 或 Temporal。
4. 当多实例并发出现后再设计分布式取消、锁和幂等键。
5. 建立离线 CI，执行 Ruff、Pytest、Alembic、前端 typecheck/build 和 Secret Scan；真实模型评测保持手动触发。

### 15.4 缺少证据的内容

- 个人独立负责或主导各模块的作者证据。
- 2025.08-2025.12 历史源码快照与当时实际模型版本。
- Qwen2.5/Qwen2.5-VL 的当前运行报告。
- BGE-Reranker 的排序收益和延迟对比。
- BGE-M3、Redis、PostgreSQL 长期记忆的集成测试与运行报告。
- 最新源码的完整质量门禁结果。
- 并发吞吐、用户规模、生产环境稳定性和业务转化数据。
- CI/CD、OpenTelemetry、Prometheus、Kubernetes、MCP、Kafka、Temporal 等实现证据。
